{
  "name": "court-edge-pro",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8080",
    "build:live": "node live-board.mjs",
    "verify": "node --test tests/*.test.mjs && node tests/static-check.mjs"
  }
}
