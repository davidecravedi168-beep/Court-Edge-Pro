import test from 'node:test';import assert from 'node:assert/strict';import {devigTwoWay,expectedValue,robustProbability,gateDecision,brierScore,closingLineValue,analyzeMarket} from '../quant-engine.mjs';
test('devig sums to one',()=>{const x=devigTwoWay(1.91,1.91);assert.ok(Math.abs(x.a+x.b-1)<1e-12);assert.ok(x.overround>0)});
test('EV formula',()=>assert.ok(Math.abs(expectedValue(.60,1.80)-.08)<1e-12));
test('robust probability applies penalties',()=>assert.ok(robustProbability(.62,{uncertainty:.08,disagreement:.04,dataQuality:90,availabilityPenalty:.01,sampleSize:20})<.62));
test('availability gate can fail closed when required',()=>{const g=gateDecision({robustEV:.05,rawEV:.06,dataQuality:90,disagreement:.02,uncertainty:.02,marketBooks:5,oddsAgeMin:5,availabilityStatus:'UNKNOWN',requireAvailability:true,sampleSize:40,modelMarketGap:.04,marketIntegrity:true});assert.equal(g.pass,false);assert.ok(g.reasons.includes('AVAILABILITY_UNKNOWN'))});
test('limited availability may continue with penalty when policy allows',()=>{const g=gateDecision({robustEV:.05,rawEV:.06,dataQuality:90,disagreement:.02,uncertainty:.02,marketBooks:5,oddsAgeMin:5,availabilityStatus:'LIMITED',requireAvailability:false,sampleSize:40,modelMarketGap:.04,marketIntegrity:true});assert.equal(g.pass,true)});
test('anomalous model-market gaps fail closed',()=>{const g=gateDecision({robustEV:.35,rawEV:.60,dataQuality:92,disagreement:.02,uncertainty:.03,marketBooks:7,oddsAgeMin:3,availabilityStatus:'VERIFIED',requireAvailability:false,sampleSize:50,modelMarketGap:.31,marketIntegrity:true});assert.equal(g.pass,false);assert.ok(g.reasons.includes('MODEL_MARKET_GAP'));assert.ok(g.reasons.includes('EV_ANOMALY'))});
test('challenger disagreement can fail closed',()=>{const g=gateDecision({robustEV:.05,rawEV:.08,dataQuality:90,disagreement:.02,challengerGap:.09,uncertainty:.03,marketBooks:6,oddsAgeMin:3,availabilityStatus:'VERIFIED',requireAvailability:false,sampleSize:40,modelMarketGap:.05,marketIntegrity:true});assert.equal(g.pass,false);assert.ok(g.reasons.includes('CHALLENGER_GAP'))});
test('thin model sample fails closed',()=>{const g=gateDecision({robustEV:.05,rawEV:.08,dataQuality:90,disagreement:.02,uncertainty:.03,marketBooks:6,oddsAgeMin:3,availabilityStatus:'LIMITED',requireAvailability:false,sampleSize:5,modelMarketGap:.05,marketIntegrity:true});assert.equal(g.pass,false);assert.ok(g.reasons.includes('MODEL_SAMPLE'))});
test('brier known value',()=>assert.ok(Math.abs(brierScore([{prob:.8,outcome:1},{prob:.3,outcome:0}])-.065)<1e-12));
test('positive CLV',()=>assert.ok(closingLineValue(2.05,1.95)>0));
test('analysis accepts consensus market probability and is bounded',()=>{const x=analyzeMarket({engineProbs:[.62,.60,.61],engineWeights:[1.2,1,1],oddsPick:1.82,oddsOther:2.06,marketProb:.54,overround:.045,uncertainty:.025,disagreement:.03,dataQuality:92,marketBooks:5,oddsAgeMin:10,availabilityStatus:'VERIFIED',requireAvailability:false,marketStability:90,sampleSize:45});assert.ok(x.modelProb>0&&x.modelProb<1);assert.equal(x.marketProb,.54);assert.ok(x.confidence>=0&&x.confidence<=100);assert.ok(['PAPER BET','WAIT'].includes(x.decision));assert.ok(x.stakeUnits<=.50)});
